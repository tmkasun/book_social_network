source 'https://rubygems.org'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.0.0'

#gem 'mandrill-api'                      # Use mandrill as the transaction mail sender
gem 'mail'                                # Mail is an internet library for Ruby that is designed to handle emails generation, parsing and sending in a simple
gem 'email_validator'                     # new improved with strict validator https://github.com/balexand/email_validator

#gem 'pg'                                  # Use postgre as the database for Active Record
#gem 'activerecord-postgres-hstore'        # Speed up hashes in the database.

gem 'mysql2'                               #add suport to mysql2 database

#gem 'devise', '~> 3.1.0'                  # Flexible authentication solution for Rails with Warden

#gem 'lazy_high_charts'                    # Highcharts is a charting library written in pure HTML5/JavaScript
#gem 'sass-rails', '~> 4.0.0'              # Use SCSS for stylesheets

gem 'uglifier', '>= 1.3.0'                # Use Uglifier as compressor for JavaScript assets
#gem 'coffee-rails', '~> 4.0.0'            # Use CoffeeScript for .js.coffee and execjs for .js assets and views
gem 'execjs'
gem 'therubyracer'

# See https://github.com/sstephenson/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# Use jquery as the JavaScript library
#gem 'jquery-rails'

=begin
dynamic_attributes is a gem that lets you dynamically specify attributes on ActiveRecord models, which will be serialized and deserialized to a given text column. Dynamic attributes can be defined by simply setting an attribute or by passing them on create or update.
=end
#gem 'dynamic_attributes'

# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 1.2'

#FasterCSV is intended as a replacement to Ruby‘s standard CSV library. It was designed to address concerns users of that library 
#gem 'fastercsv'

# Use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# Use unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]

group :test, :development do
  gem 'annotate', '~> 2.5.0'                        # Annotates Rails/ActiveRecord Models, routes, fixtures based on schema
end
